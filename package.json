{
  "name": "foobar",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@google/maps": "^0.5.5",
    "aws-sdk": "^2.324.0",
    "dotenv": "^6.0.0",
    "uuid": "^3.3.2"
  },
  "scripts": {
    "start": "cd ./react-app && npm start",
    "setup": "node ./aws-setup.js",
    "cleanup": "node ./aws-cleanup.js",
    "build": "cd ./react-app && npm run build"
  }
}
